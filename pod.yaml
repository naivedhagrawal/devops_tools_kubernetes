metadata:
  labels:
    some-label: agent
spec:
  containers:
  - name: linux
    image: ubuntu
    command:
      - /bin/bash
      - sudo apt-get update
    tty: true
    env:
    - name: CONTAINER_ENV_VAR
      value: busybox