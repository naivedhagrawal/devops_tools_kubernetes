metadata:
  labels:
    some-label: agent
spec:
  containers:
  - name: linux
    image: alpine
    command:
      run:
      - apk add make
    tty: true
    env:
    - name: CONTAINER_ENV_VAR
      value: busybox